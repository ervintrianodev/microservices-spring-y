#CONFIGURACION DEL USUARIO SERVICE
server:
  port: 8080

spring:
  application:
    name: usuarios-service

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost
